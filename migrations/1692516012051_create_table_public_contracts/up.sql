CREATE TABLE "public"."contracts" ("id" uuid NOT NULL DEFAULT gen_random_uuid(), "user_id" uuid NOT NULL, "client_id" uuid NOT NULL, "type" text NOT NULL, "fixed_contract_amount" integer NOT NULL, "hourly_contract_rate" integer NOT NULL, "description" text NOT NULL, "title" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("user_id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("client_id") REFERENCES "public"."users"("user_id") ON UPDATE restrict ON DELETE restrict);COMMENT ON TABLE "public"."contracts" IS E'table to store contract detail';
CREATE EXTENSION IF NOT EXISTS pgcrypto;
